# Proposal: Documentation Alignment

## Why

The repository currently contains placeholder documentation from the `rust-openspec-starter` template. In addition, the roadmap and deferred work need to be formalized in a `BACKLOG.md` file to strictly follow the OpenSpec discipline defined in `AGENTS.md`.

## What Changes

- Replaces the generic `README.md` with Pacta's actual identity and architectural philosophy.
- Tweaks `PROJECT.md` to reference the backlog.
- Creates `BACKLOG.md` to serve as the source of truth for the project Roadmap (Phases 1-4) and deferred work.
