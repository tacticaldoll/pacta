# Tasks: Pacta Foundation

- [x] Write `AGENTS.md` to define the architectural axioms, the commit workflow, and the adversarial review stance.
- [x] Write `PROJECT.md` to define the core purpose of Pacta.
- [x] Create the Cargo workspace (`Cargo.toml`).
- [x] Create `crates/pacta-contract` with a zero-dependency configuration.
- [x] Create `crates/pacta-governance` with `tianheng` rules enforcing the contract's isolation.
- [x] Run the Definition of Done (build, format, clippy, tianheng check).
- [x] Update `BACKLOG.md` (or equivalent) with the ✓ shipped status after archiving.
