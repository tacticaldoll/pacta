## 1. Record the decision (ADR)

- [x] 1.1 Add `docs/adr/0004-retire-openspec-change-archive.md` — supersedes ADR-0002's archival clause; states the sync-not-archive lifecycle and rationale
- [x] 1.2 Add a Status note to `docs/adr/0002-adopt-openspec.md` pointing to ADR-0004 (leave its decision text intact as the historical record)

## 2. Update the lifecycle prose

- [x] 2.1 `AGENTS.md`: change the flow to `explore -> propose -> apply -> sync`; make Sync terminal (promote delta specs into `openspec/specs/`, then remove the change directory); drop the Archive step
- [x] 2.2 `docs/development-flow.md`: replace the `openspec archive` step with sync + `git rm -r` the change directory
- [x] 2.3 `docs/blueprint.md`: drop "and archive" from the lifecycle sentence

## 3. Remove the archive

- [x] 3.1 `git rm -r openspec/changes/archive/` (all 22 archived change directories; recoverable from git history)

## 4. Spec sync (delta -> living specs, by hand)

- [x] 4.1 Sync the `governance-prose` MODIFIED requirement into `openspec/specs/governance-prose/spec.md` (rename the scenario to "Governance ignores historical prose", citing git history / superseded ADRs)
- [x] 4.2 Sync the `quality-governance` MODIFIED requirement into `openspec/specs/quality-governance/spec.md` ("Historical prose is not governed" cites git history / superseded ADRs)

## 5. Definition of Done

- [x] 5.1 `openspec validate --all --strict` passes with no archive present
- [x] 5.2 `openspec list` still works (no active-change breakage)
- [x] 5.3 `cargo run -p pacta-governance -- check --manifest-path Cargo.toml` (clean — active prose still passes)
- [x] 5.4 `cargo test -p pacta-governance` (prose/governance tests still pass)
- [x] 5.5 Confirm no remaining archive-step reference in the change-lifecycle prose (`AGENTS.md`, `docs/development-flow.md`, `docs/blueprint.md`) or the two reworded spec scenarios. Out of scope: the six `TBD ... Update Purpose after archive` spec Purpose placeholders (pre-existing cruft, stale since those specs were created) are handed to the release-readiness sweep; and the OpenSpec CLI shims under `.claude/`/`.agent/` are generated by `openspec update` and left as-is (see design)
- [x] 5.6 Retire this change: sync its deltas (task 4), then `git rm -r openspec/changes/dissolve-openspec-archive/` — no archive entry
